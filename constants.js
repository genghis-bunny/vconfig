/**
 * Add magic string and numbers etc here.
 */
const constants = {
	CRITICAL: 'CRITICAL',
	WARNING: 'WARNING',
	FIXME: 'FIX ME',
	EXPECTING_CONFIG_OBJ: 'Expecting config parameter to be object',
	EXPECTING_SCHEMA_OBJ: 'Expecting schema parameter to be object',
	EXPECTING_CONFIG_PATH: 'Expecting [config] parameter to be string which is the path to a json config file',
	EXPECTING_SCHEMA_PATH: 'Expecting [schema] parameter to be string which is the path to a json schema file',
	OPTIONS_SET: 'Expecting options to be set',
	ZSCHEMA_SET: 'Expecting zschema to be set'
};

module.exports = constants;
